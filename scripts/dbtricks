#!/usr/bin/env perl

use Modern::Perl;

use lib 'lib/';
use Model::Schema;

my $MODEL = Model::Schema->connect('dbi:SQLite:konekt.db');

my @results = $MODEL->resultset('Topic')->all;

say $_ for @results;
